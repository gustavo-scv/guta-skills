# Claude Code Notes

This repo stores portable skills. To use one from Claude Code, copy the full skill directory from `skills/<skill-name>/` into `~/.claude/skills/<skill-name>/`.

For repository maintenance, follow `AGENTS.md` and keep `registry.json`, each `skill.json`, and each `SKILL.md` in sync.
